import joblib
from sklearn.ensemble import RandomForestClassifier

# Assume you have your data split
model = RandomForestClassifier()
model.fit(X_train, y_train)

joblib.dump(model, 'best_model.pkl')  # This file must be pushed to GitHub
